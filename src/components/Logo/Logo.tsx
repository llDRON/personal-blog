import React from 'react'
import './Logo.css'

type Props = {}

const Logo = (props: Props) => {
    return (
        <div className="logo">
            Blog <span className="logo-span">template</span>
        </div>
    )
}

export default Logo
